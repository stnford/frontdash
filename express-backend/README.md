1. change directory into express-backend (cd express-backend)
2. `npm install`
3. `npm start`

note: you might get an error if you do not first run 'docker compose up' in db folder 
    - so cd db
    - docker compose up